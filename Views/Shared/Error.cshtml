@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">

            <div class="alert alert-danger shadow-sm">
                <h2 class="text-danger">
                    <i class="bi bi-exclamation-triangle-fill"></i> Oops! Đã xảy ra lỗi.
                </h2>
                <p class="mb-0">Chúng tôi rất tiếc vì sự cố này. Vui lòng thử lại sau hoặc quay về trang chủ.</p>
            </div>

            @if (Model.ShowRequestId)
            {
                <div class="card mt-3 shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title">Mã yêu cầu (Request ID)</h5>
                        <p class="card-text">
                            <code>@Model.RequestId</code>
                        </p>
                        <p class="text-muted small">
                            Mã này giúp chúng tôi kiểm tra lỗi dễ hơn.
                        </p>
                    </div>
                </div>
            }

            <div class="mt-4">
                <a href="/" class="btn btn-primary">
                    <i class="bi bi-house-door"></i> Về trang chủ
                </a>
            </div>

            <hr class="mt-4" />

            <h4>Thông tin dành cho nhà phát triển</h4>
            <p class="text-muted">
                Để xem chi tiết lỗi, hãy bật môi trường <strong>Development</strong>.
            </p>
            <p class="text-muted">
                Thiết lập biến môi trường:
                <br />
                <code>ASPNETCORE_ENVIRONMENT = Development</code>
            </p>
            <p class="text-warning small">
                ⚠️ Không bật chế độ Development trên môi trường sản xuất (deploy).
            </p>

        </div>
    </div>
</div>
